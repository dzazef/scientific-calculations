#include <float.h>
#include <stdio.h>

int main() {
    printf("Float max: %e\n", FLT_MAX);
    printf("Double max: %e\n", DBL_MAX);
    printf("Float eps: %e\n", FLT_EPSILON);
    printf("Double eps: %e\n", DBL_EPSILON);
    return 0;
}
